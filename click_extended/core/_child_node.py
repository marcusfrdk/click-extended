"""Child node class that stores information about the decorator."""

from abc import ABC, abstractmethod


class ChildNode(ABC):
    """Child node class that stores information about the decorator."""
